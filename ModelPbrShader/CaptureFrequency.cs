namespace Game {
    /// <summary>
    /// 环境捕获频率档位（值匹配 ModelPbrShaderSettings 中的预设数组下标）
    /// </summary>
    public enum CaptureFrequency {
        Low = 0,
        Medium = 1,
        High = 2
    }
}
